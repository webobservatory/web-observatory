!!! 5
html(lang='en')
  include includes/head
  body
    include includes/nav
    #wrap
      #content.container
        .row
          a.btn.btn-primary.btn-large(href='#myModal', role='button', data-toggle='modal') Add a dataset
          // Modal
          #myModal.modal.hide.fade(tabindex='-1', role='dialog', aria-labelledby='myModalLabel', aria-hidden='true')
            .modal-header
              button.close(type='button', data-dismiss='modal', aria-hidden='true') &times;
              h3#myModalLabel Add a new dataset
            .modal-body
              form#myform.form-horizontal(action='', method='post')
                fieldset
                  input#update(type='hidden', name='update', value='')
                  // Text input
                  .control-group
                    label.control-label(for='Title') Title
                    .controls
                      input#Title.input-xlarge(type='text', placeholder='Title of the dataset', required='')
                  // Text input
                  .control-group
                    label.control-label(for='addr') Address
                    .controls
                      input#addr.input-xlarge(type='text', placeholder='The URL where your dataset can be accessed', required='')
                  // Select Basic
                  .control-group
                    label.control-label(for='type') Interface
                    .controls
                      select#type
                        option SPARQL
                        option MangoDB
                        option RAGLD
                  // Textarea
                  .control-group
                    label.control-label(for='desc') Description
                    .controls
                      textarea#desc.input-xlarge(placeholder='A brief description of your dataset')
                  // Text input
                  .control-group
                    label.control-label(for='Creator') Creator
                    .controls
                      input#Creator.input-xlarge(type='text', placeholder='The publisher of this dataset')
                  .control-group
                    label.control-label(for='Creator') Email
                    .controls
                      input#Email.input-xlarge(type='text', placeholder='The email of the publisher')
            .modal-footer
              button#cancel.btn(data-dismiss='modal', aria-hidden='true') Cancel
              button#submit.btn.btn-primary Submit
          div(style='height:30px')
        .row
          table#display.table.table-bordered.table-hover
            thead
              tr
                th Title
                th Type
                th Description
            tbody#tablebody
              each val, index in table
                tr
                  td.span4
                    a.tp(href=val.url interface=val.type data-toogle='tooltip' title=val.url) 
                      = val.title
                  td.span1 
                    = val.type
                  td 
                    = val.desc
            tfoot
              tr
                th Title
                th Type
                th Description
        #query-frame.row  
      #push
    include includes/footer

