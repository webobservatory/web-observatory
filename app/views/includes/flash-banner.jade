if info
  for infomsg in info
    .alert.alert-success
      button.close(type='button' data-dismiss='alert') &times;
      = infomsg
if error
  for infomsg in error
    .alert.alert-error
      button.close(type='button' data-dismiss='alert') &times;
      strong Error! 
      = infomsg
