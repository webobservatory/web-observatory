extends base

block content
    include includes/loginbox

block footer
    script.
        $('button.signin').bind('click',function(event){
            event.preventDefault();
            $('#loginform').attr('action',$(this).attr('target')).submit();
        })